<%= form_with(model: note) do |form| %>
  <% if note.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(note.errors.count, "error") %> prohibited this note from being saved:</h2>

      <ul>
        <% note.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :note_title, style: "display: block" %>
    <%= form.text_field :note_title %>
  </div>

  <div>
    <%= form.label :note_author, style: "display: block" %>
    <%= form.text_field :note_author %>
  </div>

  <div>
    <%= form.label :note_contents, style: "display: block" %>
    <%= form.text_area :note_contents %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
